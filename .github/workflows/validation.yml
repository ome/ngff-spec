name: Validation
on:
  push:
  pull_request:

jobs:
  validate:
    name: Validation
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v9.0.0
      - run: uv run pytest -v
